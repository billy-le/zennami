import { PlayerState } from "./types/player-state";

export const DEFAULT_PLAYER_STATE: PlayerState = {
  isPlaying: false,
  currentStation: null,
  volume: 0.5,
};

export const USER_AGENT = "ZenNami/0.0.1";
